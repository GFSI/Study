//Mostrar todos los numeros impares del 1 al 10 sin hacer simplemente console.log en cada numero

let index = 0
for (index; index <= 10; index++) {
    if ((index % 2) != 0) {
        console.log(index)
    }
}
